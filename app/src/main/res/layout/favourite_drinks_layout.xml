<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                        xmlns:app="http://schemas.android.com/apk/res-auto"
                                        android:id="@+id/drawer_layout"
                                        android:layout_width="match_parent"
                                        android:layout_height="match_parent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/parent_layout"
        android:background="@drawable/gradient_background"
        android:orientation="vertical">

        <include layout="@layout/drinks_recycler_view"/>

        <TextView
            android:id="@+id/bottom_textView"
            android:background="@drawable/toolbar_gradient_background180"
            android:textSize="16sp"
            android:textColor="@color/light_color"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:gravity="center_vertical|center_horizontal"
            android:layout_alignParentBottom="true"
            android:text="@string/swipe_to_remove"/>

        <TextView
            android:id="@+id/empty_disclaimer_textView"
            android:textSize="16sp"
            android:textColor="@color/textView_color"
            android:visibility="gone"
            android:text="Fav drinks list is empty"
            android:gravity="center_horizontal|center_vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar_drinks_list"
            app:title="Favourite drinks"
            app:titleTextColor="@color/light_color"
            style="@style/CustomToolbar"/>

    </RelativeLayout>

    <FrameLayout
        android:id="@+id/content_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.design.widget.NavigationView
        android:id="@+id/nav_view"
        style="@style/CustomDrawerNavigationView"
        app:headerLayout="@layout/navigation_menu_header"/>

</android.support.v4.widget.DrawerLayout>